configuration:
  endpoint: http://localhost:8085/configurations/entries
  port: 8085
  local: localhost
  baseUrl: marccat

logging:
  level:
    root: ERROR
    org.springframework: ERROR
    net.sf.hibernate: ERROR
    net.sf.hibernate.SQL: DEBUG
    org.folio.marccat.integration.search: DEBUG
    ru.yandex: ERROR
    org.folio.marccat: INFO

server:
  port: 8080
